import Container from "../components/layout/Container";

const Explorer = () => {
  return (
    <Container className="py-10">
      <h1 className="text-3xl font-bold">Credential Explorer</h1>
      <p className="mt-4 text-muted">
        Browse and search for credentials on the network.
      </p>
      {/* Add explorer functionality here */}
    </Container>
  );
};

export default Explorer;
